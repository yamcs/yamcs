{
  "private": true,
  "scripts": {
    "build": "wsrun --stages build",
    "watch": "wsrun --parallel watch"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/*"
    ]
  },
  "devDependencies": {
    "wsrun": "^5.0.0"
  }
}
