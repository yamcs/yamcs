services:
  - class: org.yamcs.http.HttpServer
    args:
      port: 9191
      webSocket:
        maxFrameLength: 1048576

instances:
  - LongWebsocketFrameTest

incomingDir: /tmp/yamcs-incoming
dataDir: /tmp/yamcs-LongWebsocketFrameTest-data

secretKey: testtest
